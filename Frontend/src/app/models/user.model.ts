/* ========== USER MODEL ========== */

/** Minimal user info to keep on frontend */
export interface User {
  id: number;
  email: string;
  firstName: string;
  lastName: string;
  // add more fields as your API returns them
}
